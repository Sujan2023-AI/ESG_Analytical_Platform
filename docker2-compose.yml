services:
  web:
    build:
      context: .
      dockerfile: ontology-graphdb/retriever
    ports:
      - "3902:3902"
    volumes:
      - .:/app  # Optional: live-reloading during dev
    environment:
      - FLASK_ENV=development  # Optional: for Flask dev mode